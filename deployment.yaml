apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault-demo-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/blinskyad/vault-test-app.git
    targetRevision: master
    path: .
  destination:
    server: https://kubernetes.default.svc
    namespace: default
